# Base desktop app dependencies.
# Safe default install for Windows and macOS.
#
# Optional backends:
#   python -m pip install -r requirements-pt.txt
#   python -m pip install -r requirements-tflite.txt

Pillow>=10.0.0
numpy>=1.26.0
opencv-python>=4.10.0.84
ttkbootstrap>=1.10.1
PyYAML>=6.0.1
