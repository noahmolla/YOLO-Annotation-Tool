# ============================================
# YOLO Annotator - Requirements
# ============================================

# Core Dependencies (Required)
Pillow>=9.0.0
numpy>=1.21.0
opencv-python>=4.5.5
ttkbootstrap>=1.10.0
PyYAML>=6.0

# ============================================
# MODEL INFERENCE (at least one required)
# ============================================

# PyTorch YOLO models (.pt files) - RECOMMENDED
# Supports YOLOv5, YOLOv8, YOLOv11, YOLO26, etc.
ultralytics>=8.0.0

# ============================================
# OPTIONAL: TensorFlow Lite (.tflite files)
# Uncomment ONE of the lines below if needed:
# ============================================

# Full TensorFlow (~500MB):
# tensorflow>=2.8.0,<2.18.0

# OR tflite-runtime only (~5MB, may need manual install on Windows):
# tflite-runtime>=2.5.0

# ============================================
# NOTES
# ============================================
# - Most users only need ultralytics (above) for .pt models
# - TensorFlow/tflite is only needed for .tflite model files
# - The app auto-detects model type by file extension
# - On Windows, tflite-runtime may need manual wheel installation
#   See: https://www.tensorflow.org/lite/guide/python
