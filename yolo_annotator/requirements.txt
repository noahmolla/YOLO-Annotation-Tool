# ============================================
# YOLO Annotator - Requirements
# ============================================

# Core Dependencies (Required)
Pillow>=9.0.0
numpy>=1.21.0
opencv-python>=4.5.5
ttkbootstrap>=1.10.0
PyYAML>=6.0

# ============================================
# MODEL INFERENCE OPTIONS
# Choose ONE or BOTH depending on your needs
# ============================================

# OPTION A: PyTorch YOLO models (.pt files) - RECOMMENDED
# Supports YOLOv5, YOLOv8, YOLOv11, etc.
# Best for most users - easy to use pretrained models
ultralytics>=8.0.0

# OPTION B: TensorFlow Lite models (.tflite files)
# Choose ONE of the following:

# B1: Full TensorFlow (Recommended - easier to install, ~500MB)
tensorflow>=2.8.0,<2.18.0

# B2: tflite-runtime (Smaller ~5MB, but harder on Windows)
# Uncomment below and comment out tensorflow above if preferred:
# tflite-runtime>=2.5.0

# ============================================
# NOTES
# ============================================
# - You can use BOTH PyTorch (.pt) and TFLite (.tflite) models
# - The app automatically detects model type by file extension
# - If you don't need TFLite, you can comment out tensorflow
# - If you don't need PyTorch, you can comment out ultralytics
# - On Windows, tflite-runtime may need manual wheel installation
#   See: https://www.tensorflow.org/lite/guide/python
